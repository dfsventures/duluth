# Molly — Product Roadmap

_Last updated: 2026-08-06 (Portfolio Ledger/Cross-Fund Views/Derived Metrics/Sheet Ingestion shipped — Part 10, WS23–WS27; sidebar navigation IA rework shipped — Part 11, WS28; sidebar visual hierarchy/accessibility/icon polish shipped — Part 12, WS29; Deal Ledger table density/navigation cues, Select/table-shell sweep, and app-wide list-spacing widen shipped — Part 13, WS30–WS32; Fund Performance Snapshot in LP Reports shipped — Part 14, WS33–WS36; Per-Fund Performance Override shipped — Part 15, WS37; Pre-Investment Due Diligence Intake shipped — Part 16, WS38–WS43; platform audit shipped — Part 17, F30–F36; Due Diligence Completion Signal shipped — Part 18, WS44; Inline Document Viewing shipped — Part 19, WS45, F37–F38; Founder Document Upload for Active Companies shipped — Part 20, WS46–WS47, F39; Awaiting-Setup Queue Hygiene shipped — Part 21, WS48, F40–F41; R2 storage incident fixed + founder-own-doc-visibility fix + branding pass + app-wide `text-muted` sweep shipped same-day; second platform audit shipped — Part 22, F42–F46; storage integrity/audit-log/docs follow-up planned, not yet built — Part 23, F42–F46, WS49–WS53, decisions Q67–Q68 confirmed; optional per-publish note on the LP report-published email shipped — Part 24, WS54, F47; dedicated security audit — Part 25, F48–F50, WS55–WS57, found + scoped, not yet built; multiple email addresses per LP shipped — Part 26, WS58–WS61, F51; confidentiality audit of the public repo — Part 27, F52–F57, findings recorded; **HIGH findings F52/F53 remediated via git history rewrite 2026-08-06**; lower-severity remediation + standing guardrail planned — Part 28, WS62–WS65, not yet built; **Founder Equity Dilution / Cap-Table Scenario Planner planned — Part 29, WS66–WS68, not yet built, one open product decision Q71**)_

This document is the source of truth for existing platform features and planned enhancements. Update it as features ship or priorities change.

> **Founder Equity Dilution / Cap-Table Scenario Planner (Part 29, WS66–WS68, 2026-08-06) — PLANNED, NOT yet built.** A new founder-facing modeling tool: a founder types their own hypothetical financing assumptions from scratch and sees a stage-by-stage ownership breakdown. Deliberately **standalone** — it never reads Molly's admin-only `Deal`/`Fund`/`PortfolioCompany` ledger; founders supply every input by hand (self-declared hypotheticals, no more sensitive than their own metrics). Original implementation of standard SAFE-note/venture-dilution mechanics — **no external tool's code is copied**. Fixed three-stage shape (optional YC-shaped accelerator with a fixed-% first tranche + optional converting second tranche → optional seed [raise + post-money] → optional Series A [% sold + post-money]), with full parity to the reference tool: multiple equal-split founders, a pre-money ESOP pool, two distinct SAFE groups (friends & family vs. pre-seed) with a per-investor **MFN** toggle (effective cap = lowest cap among non-MFN investors *in the same group*, computed dynamically), proportional dilution of all SAFEs/ESOP/accelerator tranches at each priced round, and validation warnings for impossible models. Three workstreams: **WS66** additive `CapTableScenario` model (inputs as a JSON document, the `WeeklyDigest.sections` precedent) + company-scoped CRUD routes; **WS67** a pure, DB-free, fully unit-tested `src/lib/cap-table.ts` engine (the `portfolio-metrics.ts` convention; computed breakdown is derived-on-read, never stored); **WS68** the founder UI (a `/planner` form modeled on the `/diligence` multi-section `Card` layout + a live stage-by-stage results table). No new cost line (pure arithmetic on existing Postgres/Vercel), additive-only schema, net-new surface only (no UX regression), synthetic test data throughout. **One product decision is open for Joseph — Q71: one auto-saved scenario per company (A) vs. multiple named "Base case / Optimistic seed" scenarios (B).** Felix recommends **B**; the engine is identical either way and the schema flips a single line, so WS66/WS67 can proceed while only WS68's page count waits on the answer. Full plan, math spec, findings (none — no roadmap drift found), and judgment calls in Part 29 of [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md).
>
> **Confidentiality remediation + standing guardrail (Part 28, WS62–WS65, 2026-08-06) — planned, NOT yet built.** Follow-up to the Part 27 audit. The two HIGH findings (F52/F53) were already fully remediated out of band via a `git filter-repo` history rewrite (content + commit messages scrubbed, verified clean from a fresh clone, force-pushed) — done, not in Part 28's scope. Part 28 plans the three lower-severity fixes for Alvin: **WS62** genericizes the real fund-vehicle labels (F54) — the importer's hardcoded vehicle set moves to a CLI argument (matching the existing "real data external via CLI arg" ground rule; keeps the drift-guard safety), README/schema/component comments get generic labels; **WS63** replaces the real vetted-vendor seed directory in `prisma/seed.ts` (F56) with obviously-synthetic `Acme`/`example.com` providers; **WS64** genericizes the two company-name UI placeholders on the admin company page (F57). **WS65** is the standing guardrail so this class of leak stops recurring: a written "Confidentiality & synthetic-data convention" (already added to the `docs/IMPLEMENTATION_PLAN.md` header by Felix in this pass) plus a local pre-commit hook (`scripts/hooks/pre-commit`, plain `sh`/`grep`, no new dependency) that scans staged additions against a **gitignored** `.confidential-terms` blocklist and blocks a commit on a match — the blocklist itself is never committed (a committed `.confidential-terms.example` documents the format with synthetic entries only). **Both decisions confirmed (Joseph, 2026-08-06): Q69 = pass the vehicle set as a CLI argument (keeps the drift guard); Q70 = strict block with `--no-verify` as the documented escape.** No new cost line, no UX change, additive/comment-only edits. Separately (Joseph handling himself, not an Alvin task): the docs-narrative vehicle mentions still in git history are being scrubbed via another `filter-repo` pass, same process as F52/F53.
>
> **Multiple email addresses per LP shipped 2026-08-06 — Part 26, WS58–WS61 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F51).** A Felix feasibility investigation (stopped before planning, pending Joseph's direction) became a full plan the same day, executed same-day. Joseph's confirmed direction, now live: **(D1)** any of an LP's addresses can request/verify an OTP and land in the same account with the same fund access — no single "primary" gates login; **(D2)** the opt-in report-published notification fans out to **all** of an LP's addresses, never double-mailing a person for the same fund; **(D3)** removing one of an LP's addresses does **not** sign them out — only removing their **last** address (count → zero) revokes sessions, a change from the prior revoke-on-any-email-edit behavior. Shipped: a new additive `LpEmail` table (`email` globally `@unique`, preserving "two LPs can't share an address"), backfilled 1:1 against every existing LP (18/18, verified idempotent); `LimitedPartner.email` kept as a nullable synced mirror of the primary (JC1/JC2); the two LP-OTP lookups (`/api/lp/auth/request`, `/api/lp/auth/verify`) re-pointed through `LpEmail`; a new admin sub-route `/api/admin/lps/[id]/emails` (add/remove/set-primary, audit-logged as `LP_EMAIL_ADDED`/`_REMOVED`/`_PRIMARY_CHANGED`) with `PATCH /api/admin/lps/[id]` reduced to name-only; the `/admin/lps` edit modal's single email field replaced with a manage-addresses list (primary badge, remove-with-confirm-on-last-address per JC3); and the publish-notification loop fanning out to every address. **F51** (assumption corrected during verification, confirmed true): the feared "double-mail an LP on multiple funds" case cannot occur — a publish is single-fund and `@@unique([lpId, fundId])` means each LP is visited once, so with globally-unique addresses no dedup logic was needed (and none was added). No new dependency, no new env var, no new cost line, no destructive schema change. Shipped feature detail folded into the LP portal bullet under "Existing Features" above.
>
> **Security audit (Part 25, 2026-08-06) — found, scoped, NOT yet built.** A dedicated security review (`docs/IMPLEMENTATION_PLAN.md` Part 25) turned up three items, none yet fixed: **F48 (HIGH)** — `GET /api/updates/[id]/pdf` guards with `requireAuth()` only and never scopes to the update's company, so any approved user can read any company's full investor update (body/metrics/documents) as HTML; a one-line IDOR fix (WS55) mirroring the sibling update routes. **F49 (MEDIUM)** — the F47/WS54 `escapeHtml` fix reached only `sendLpReportPublishedEmail`; ~7 other email templates still interpolate founder- and even unauthenticated-controlled text (`companyName`, `founderName`, `commenterName`, `updateTitle`) raw into admin- and invitee-facing emails, an HTML/link-injection (phishing) vector (WS56). **F50 (LOW)** — the team-invite endpoint has no rate limit, letting an authenticated OWNER drive unbounded Resend sends (WS57). Everything else audited (all 55 admin-route guards, the whole document surface, the diligence surface, auth core, the public-prefix list, the storage diagnostics, LP auth) verified clean — see Part 25's "Verified clean" section.

---

## Platform Overview

**Molly** is a portfolio management platform originally built by DFS Lab (startup accelerator/VC) to run their own portfolio, and open-sourced for other investment teams to fork. It connects three user types:

- **Founders** — portfolio company operators who submit updates and track metrics
- **Admins** — the investment team's staff who review companies, approve accounts, and analyze the portfolio
- **Investors/LPs** — read-only access via shareable tokenized links (no account required)

**Stack:** Next.js 14 App Router · Prisma ORM · PostgreSQL · S3 · Resend · NextAuth v5 · Anthropic Claude (digest drafting)

Molly is open source (MIT) with the explicit goal that other investment teams can fork and run their own instance — see the Fork Configuration item in the roadmap below.

---

## Existing Features

### Authentication & Access
- **Public homepage** (`/`) — founder-focused hero ("One place to keep your investors in the loop.") with a single primary CTA (Apply for Access); nav bar collapses to a single "Log in" link → `/login` (which already serves both founders and admins); footer carries a muted "Investor access" link → `/investors` (shipped 2026-07-03 — investors never log in, so an audience-split nav overpromised); a vertically-stacked `01./02./03.` "how it works" section replaces the old icon grid; authenticated users auto-redirect to their dashboard
- Email/password login + Google OAuth (admins restricted to a single email domain, configurable via `NEXT_PUBLIC_ADMIN_EMAIL_DOMAIN`, defaults to `dfs.vc` — see Fork Configuration below)
- **Investor Access** (`/investors`) — public page explaining that investor access is link-based (no account needed) with a support contact (`SUPPORT_EMAIL`); replaces the old "Investor Login" button, which silently ran admin Google OAuth and errored for real investors (shipped 2026-07-03)
- Founder signup → admin approval → set-password email flow, rate-limited (10/hour/IP, Postgres-backed) against `/api/auth/signup` and `/api/auth/set-password`
  - **Expired-link self-serve recovery** (shipped 2026-07-09, WS21 of Part 9 in `docs/IMPLEMENTATION_PLAN.md`) — setup links now carry a 7-day TTL (`SETUP_TOKEN_TTL_DAYS`, up from 48h) via one shared `src/lib/setup-token.ts` helper that replaced three duplicated generation sites; an expired or invalid link shows a recovery card on `/set-password` with an "Email me a new link" button, backed by a token-keyed, rate-limited (5/hour/IP) `POST /api/auth/set-password/resend` that always responds neutrally (no account-existence oracle, per JC10). Also closed F20: rejecting an approved-but-unset user now clears their outstanding token, and `/api/auth/set-password` rejects a REJECTED user's token outright.
- Middleware-enforced role-based routing (Founder → `/dashboard`, Admin → `/admin`); routing decision logic lives in `src/lib/route-access.ts` as a pure, unit-tested function with regression coverage for the Feb–Jul auth bypass
- **Transactional emails** — all 10 templates (approval, rejection, new signup, update published, update reminder, team invite, member added, weekly digest, comment notification, test email) rebuilt around the DFS brand system: Paper/Bone/Obsidian/Sky palette, Space Grotesk / IBM Plex Sans / JetBrains Mono, real DFS logo in the header, no accent bar; proper error handling on Resend API responses. **F49 (Part 25 security audit, 2026-08-06, not yet fixed):** the `escapeHtml` helper added in WS54 (F47) is applied only inside `sendLpReportPublishedEmail`; the other templates still interpolate user-controlled plain-text values (`companyName`, `founderName`, `commenterName`, `updateTitle`) raw into the HTML body — an HTML/link-injection vector into admin- and invitee-facing emails, including from unauthenticated signup input. Fix planned in Part 25, WS56 (apply the existing helper across the remaining `send*` functions).
- Login page: founder-focused with Google OAuth demoted to an admin-staff login section
- Signup page: reframed as an application form with expectation-setting copy
- **Fork Configuration** (shipped 2026-07-06, WS9) — `NEXT_PUBLIC_ORG_NAME`, `NEXT_PUBLIC_ADMIN_EMAIL_DOMAIN`, `EMAIL_LOGO_PATH`, and the existing `SUPPORT_EMAIL` now cover every UI/email spot that assumed a DFS Lab deployment (~25 occurrences across 12 files, per F14's full sweep) — a fork can rename itself and change its admin domain/logo/support contact with env vars only, no source edits. Unconfigured, output is byte-identical to before. Theming (CSS tokens, `LogoMark`, brand assets under `public/`) remains a deliberate source edit — see README's Fork Configuration section. **`ORG_NAME`'s default changed 2026-08-04** (`src/lib/org.ts`) from `"DFS Lab"` to `"DFS"` per direct branding feedback — since every email template and UI spot already routed through this one constant (WS9's whole point), this single change propagated everywhere at once; two leftover hardcoded "DFS Lab" strings on `/admin/funds` were also switched to `ORG_NAME` in the same pass. The transactional-email sender name also changed the same day to `` `Molly from ${ORG_NAME}` `` (was bare `"Molly"`) so recipients recognize it. **F44 (Part 22 audit, not yet fixed):** a second, independent copy of the `EMAIL_FROM` fallback string on `/admin/settings` (display-only) was not updated in the same pass and now shows stale text if `EMAIL_FROM` were ever unset — planned fix in Part 23, WS52 of `docs/IMPLEMENTATION_PLAN.md` (dedup only; not yet built).
- **Admin approvals queue and awaiting-setup are two distinct populations** (corrected 2026-07-09, F21 of Part 9 in `docs/IMPLEMENTATION_PLAN.md` — the previous line here claimed the opposite of what the code did) — `/admin/approvals`'s main list filters `{ status: PENDING, approvalToken: null }`, i.e. users awaiting an *admin decision*. A separate **"Awaiting password setup"** section (shipped 2026-07-09, WS22) lists approved-but-never-set-password accounts (`GET /api/admin/approvals/awaiting`, mirroring `canResendSetupLink()`) with a per-user, audit-logged (`SETUP_LINK_RESENT`) resend button (`POST /api/admin/approvals/[id]/resend`) — closing the blind spot that let a real founder's expired link go unnoticed by admins.

### Founder Features
- **Dashboard** — company summary, update history, days-since-last-update, onboarding prompt, **investor engagement card** (total investor views + last-viewed date) and a "Recent Investor Activity" list surfacing who viewed via investor links, with an empty state linking to Investor Links (shipped 2026-07-03; data source: `ShareableLinkView`, already collected, this is UI)
- **Setup Wizard** — 3-step onboarding: company profile → metric definitions → document upload. **F31 (found 2026-08-02, still open):** step 3's document-upload box is non-functional — `src/app/setup-wizard/page.tsx`'s `<input type="file" multiple>` has no `onChange` handler, so nothing is ever sent to `/api/documents/upload` or anywhere else; a founder can select files, sees no error, and nothing is uploaded. Every self-signup founder passes through this step. Not part of Part 16 (DD checklist uploads reuse the real presigned-upload flow via a purpose-built page instead, precisely because this step doesn't). Candidate for a small, isolated fix-in-place workstream: wire the same two-step presigned `POST /api/documents/upload` → S3 `PUT` flow already used elsewhere.
- **Company Profile** — name, description, website, sector, geography, funding stage, logo
- **Dynamic Sectors** — user-created sectors; admins can rename/delete
- **Metrics** — define custom metrics with units; record values over time; history table; line chart per metric
- **Updates** — rich text editor, period + title, per-update metric values, file attachments
  - Save as Draft or Publish with inline confirmation
  - **Medium-style draft composer** (shipped 2026-07-08, WS20) — `/updates/new`, the draft-edit mode on `/updates/[id]`, and the admin per-company composer were rebuilt around a borderless display-type title, a chromeless writing surface, and a slim persistent (not selection-bubble) formatting toolbar that dims until focused — the AppShell sidebar stays (Part 8, Q17 = A; this is a content-area restyle, not a full-bleed takeover). Metrics/attachments moved into a collapsed "Details" disclosure one click away. Debounced autosave (~30s, Q16 = B) runs for existing drafts only — suppressed while a manual save/publish/schedule is in flight — with an ambient "Saved · time" indicator; the new-update page still requires an explicit first save (no draft is ever auto-created). Publish is muted/disabled until period + title are filled. Every prior capability survives: template prefill, per-period metrics, attachments, publish-confirm, schedule-for-later, draft delete. `RichEditor` gained an additive `variant="chromeless"` prop (default `"boxed"`) — the templates editor and company-notes editor pass nothing and render byte-identically.
  - **Scheduled Publishing** (shipped 2026-07-06, WS11) — an optional "Schedule for later" disclosure on a draft (collapsed by default) picks a future date; a daily cron (`/api/cron/scheduled-publish`, 9am UTC) publishes it that morning via the same shared publish path as manual publishing (team email included, best-effort). Date-only granularity by design (works on any Vercel plan, no Pro-only per-minute cron needed). Drafts show a "Scheduled · {date}" badge with a "Cancel schedule" control; editing a scheduled draft keeps the schedule, manually publishing early clears it. `sentAt` is always the actual publish instant, never the scheduled date, so the 3-day edit window and cadence detection are unaffected. The two existing publish paths (create-with-SENT, and PATCH-to-SENT) and the new cron now share one `publishUpdate()` helper (`src/lib/publish-update.ts`); the dead `POST /api/updates/[id]/send` endpoint (F13 — unreachable from any client, and it skipped the team email) was deleted rather than left as a drift trap.
  - Email sent to the admin team (`TEAM_EMAIL` env var) on publish (includes metrics table + full body)
  - Edit mode for drafts; view mode with HTML rendering for all
  - **Start from a template** — optional dropdown (only shown if admin-created templates exist) prefills the update body; asks for confirmation before overwriting existing draft text (shipped 2026-07-03, see Update Templates below)
  - Comments (shared with admins) — with email notifications: admins are emailed when a founder comments, founders are emailed when an admin comments
  - PDF download
- **Investor Links** — tokenized read-only links with period date range and expiry (7d/30d/90d/1yr/never)
  - Email gate on first visit (server-validated email format as of 2026-07-03); silent re-tracking via localStorage
  - View log with email + timestamp
  - Revoke links
  - **Fixed 2026-07-06 (F15, found live during WS12 verification)**: the `/share/[token]` page itself was public, but the client-side data fetch it makes to `/api/share/[token]` (and the `/view` email-gate endpoint) was not on the middleware's public-path list — so every investor (who has no account by design) had that fetch 307-redirected to `/login`, and the share page never actually rendered any data. This had likely been broken since the route-access.ts extraction; no links existed in production to have surfaced it via a bug report. Fixed the same way as the prior `/api/cron` and `/brand` gaps (added `/api/share` to `PUBLIC_PREFIXES` in `src/lib/route-access.ts`, a token-gated route needs no session-based gate); regression test added.
- **Team Management** (`/team`) — OWNER founders can invite teammates by email (MEMBER/VIEWER roles)
  - New users get an account automatically (APPROVED + set-password link, 7-day expiry); no admin review queue
  - Existing users get a notification email and are added immediately
  - Handles edge cases: re-invites rejected users, regenerates expired tokens, detects duplicates
  - Role change (Editor ↔ Viewer) and remove member in-page
  - Admins can also add members from the company detail page (same invite flow)
- **Due Diligence checklist** (`/diligence`, shipped 2026-08-03, Part 16, WS38–WS40) — Molly's first pre-investment concept: an admin who has already decided to invest can create a company that isn't a portfolio company yet (`Company.stage: "DILIGENCE"`) and invite its founder directly. That founder gets full, real Molly access immediately — dashboard, updates, metrics, team — with a small, non-blocking checklist card on `/dashboard` (Q55: never a gate) linking to `/diligence`: a Yes/No US-incorporation question, two Stellar-ecosystem essay questions (only when the deal is Stellar-flagged), and five document uploads (cap table, bank statements, certificate of incorporation, business license, founder passport — passport required, the rest optional), reusing the existing presigned-upload flow. Passport and bank-statement uploads are tagged `isInternal: true` automatically. Sidebar item and `/diligence` itself are only reachable while `company.stage === "DILIGENCE"`; the checklist and banner disappear the moment an admin promotes the company. **Completion signal (F36, Part 18, WS44, shipped 2026-08-04):** the banner and `/diligence`'s status line originally showed the same "a few more steps" copy regardless of actual progress, and nothing notified anyone when a founder finished — found live when a founder emailed asking if it was a bug after finishing all 4 required items. Both surfaces now switch to "All done! We are reviewing your documents and will be in touch soon." once `completedAt` is set (button relabels "Continue" → "View"), and a founder confirmation email plus an admin `TEAM_EMAIL` notification fire exactly once at the real completion moment. Full plan, findings (F30–F33, F36), and decisions (Q53–Q60, Q61–Q64, every Felix recommendation accepted) in Parts 16 and 18 of `docs/IMPLEMENTATION_PLAN.md`. **Resolved 2026-08-04 (Part 20, WS46–WS47):** `/diligence` itself is untouched and still `return null`s the instant `stage !== "DILIGENCE"` (`src/app/diligence/page.tsx:208–210`, by design — it's the DD-specific checklist, not a general document surface), but a founder on a promoted (`ACTIVE`-stage) company is no longer left with no way to re-upload documents — see the new **Company Documents** bullet below.
- **Company Documents** (`/company/documents`, shipped 2026-08-04, Part 20, WS46–WS47) — general document library for founders: upload any of the 10 `DOC_TYPES` (the original 5 plus the DD-specific 5 — cap table, bank statements, certificate of incorporation, business license, passport), and view (inline for PDF/PNG/JPEG/GIF/WebP) or download every non-`isInternal` document the company has, including ones an admin uploaded on the founder's behalf — reusing `POST /api/documents/upload` and `GET /api/companies/[id]/documents` as-is, no new endpoints. Unlike `/diligence`, not gated by `Company.stage` (Q55's "full, non-blocking access from day one" precedent extends here too — DD and ACTIVE founders alike see it) and always in the founder sidebar. Upload/view/download only — no archive/delete affordance (Q65 = A, Felix's recommendation accepted); a founder who needs something removed or retyped asks an admin. Resolves a real incident: a founder's 5 DD document uploads never actually saved because of a since-fixed R2 storage-credentials bug, and once their company was promoted to `ACTIVE`, `/diligence` self-disabled with no other document surface anywhere in the app. **Also closed in the same pass (F39):** `PATCH /api/documents/[id]` (archive/retype) had no role or ownership check at all — any founder member of a company could call it directly, no UI involved, to archive/unarchive/retype any document belonging to their company, including `isInternal`-flagged ones like a teammate's passport; now `requireAdmin()`-gated, an unconditional endpoint-level fix confirmed by a new regression test, not merely hidden from the UI. **Fixed same-day (2026-08-04, found live via a real founder stuck re-uploading their passport repeatedly, believing it wasn't working):** the page's own general document list (`GET /api/companies/[id]/documents`) had no exception for the uploader's own `isInternal` documents — a founder could never see their own passport/bank-statement uploads on this page, no matter how many times they retried, since the original WS42/F33 filter was a blanket `isInternal: false` for non-admins. Now `isInternal: false OR uploadedById: me`, so a founder sees a "Received" badge (name/date, proof it arrived) on their own internal uploads with View/Download still suppressed — never a teammate's internal upload, which the server query still excludes entirely for non-admins. Verified in Part 22's audit (F42–F46 below): both content-serving routes (`GET /api/documents/[id]`, `GET /api/documents/[id]/view`) are untouched and still 403 a non-admin on any `isInternal` document regardless of uploader. Full plan, findings, and decisions (Q65) in Part 20 of `docs/IMPLEMENTATION_PLAN.md`.

### Admin Features
- **Dashboard** — KPI cards (total companies, pending approvals, updates this month, "Behind on updates" companies); 6-month published updates bar chart; sector breakdown; collapsible overdue companies table with days-since color coding
  - **Smart "needing attention" detection** — cadence-based logic: grace period for companies < 30 days old; flagged if 30+ days old with < 3 published updates; for 3+ updates, flagged only if time since last update exceeds average gap from last 5 updates
  - **Unapproved companies filtered** — companies from pending (not yet approved) founders no longer appear in admin list or dashboard stats
- **Approvals** (`/admin/approvals`) — approve/reject signups with email notifications; a separate "Awaiting password setup" section (shipped 2026-07-09, WS22) surfaces approved-but-never-set-password accounts with a per-user, audit-logged resend. **Queue hygiene shipped 2026-08-04 (Part 21, WS48, F40–F41):** "Awaiting password setup" rows can be dismissed, or deleted outright when safe, instead of accumulating indefinitely. A per-row "Dismiss/Undismiss" (safe, reversible, no email, audit-logged) removes a row from the active view without touching login/resend eligibility; rows also auto-group into a collapsed "Dismissed / stale" section once 30+ days past their own token's expiry, with no admin action needed. A guarded hard "Delete account" is offered only when `canHardDeleteFromQueue()` confirms it's safe — never a company's creator (`Company.createdById` is a required, non-cascading FK — F41) and never an admin; blocked otherwise with an inline explanation rather than a disabled button.
- **Company Management** — grid view, search, add company, bulk CSV import
- **Company Detail** — full editable profile; view company updates; manage members; metric charts + history; document management with type tagging, search, filtering, and archive
- **Settings** (`/admin/settings`) — email trigger overview, FROM address display, RESEND_API_KEY configuration check, "Send Test Email" button with live success/error feedback
- **Update Reminders** — Vercel Cron job (daily, 9am UTC) sends reminder emails to founders when updates are overdue
  - Per-company frequency: weekly / bi-weekly / monthly / quarterly / disabled
  - Cooldown via `lastReminderSentAt` prevents repeat emails within the configured window
  - Emails all OWNER + MEMBER founders on the company; secured by `CRON_SECRET`
  - **Fixed 2026-07-03**: two stacked bugs meant this had likely never fired since it shipped — (1) the route only exported `POST` but Vercel Cron invokes with GET, and (2) even after fixing that, auth middleware redirected every cron invocation to `/login` before the route's own `CRON_SECRET` check ever ran, since cron requests carry no session. `/api/cron` is now on the middleware's public-path list (the route's `CRON_SECRET` check remains the real authorization gate); both fixes verified live against production.
  - **Fixed 2026-07-06 (F12, WS10.1)**: the cooldown now only advances when at least one reminder email actually sends for a company — previously a broken Resend key still burned the full cooldown window on every "attempt". Self-healing: once `RESEND_API_KEY` is rotated, the next run retries every still-overdue company instead of waiting out an already-burned window.
- **Metric Alerts** (admin dashboard, shipped 2026-07-06, WS10) — daily cron (`/api/cron/alerts`, 9:30am UTC) evaluates two plain-arithmetic rules per company: a metric changed ≥20% (either direction, env-tunable via `METRIC_ALERT_CHANGE_PCT`) between its two most recent recorded dates, or the last 3 published updates all shipped with zero metrics attached. Fired alerts dedupe atomically on a unique key (no repeat alerts for unchanged data) and surface in a dismissible "Metric Alerts" section on `/admin` (only rendered when alerts exist); dismissing writes an `ALERT_DISMISSED` audit row. Admin-only, no email in v1 — dashboard-only by design so it isn't gated on the Resend outage.
- **Update Templates** (`/admin/templates`) — admin-created reusable skeletons (rich-text body + name/description) founders can start an update from; soft-delete via archive/unarchive; audit-logged (shipped 2026-07-03)
- **Investor Links** — multi-company link creation; full view log; revoke
  - **Bulk LP Report Link** (shipped 2026-07-06, WS12) — "Bulk select by period" in the admin builder: pick a From/To date range and one-click-select every published update in range across the whole portfolio, with a running "N updates across M companies" summary; an optional "Limit the metric summary to this period" checkbox (checked by default after a bulk select) adds an additive `metricScope` field (`ALL_TIME` default / `PERIOD`) on `ShareableLink` — `PERIOD` scopes the share page's per-company metric summary to the link's date range instead of showing each metric's latest all-time value. Every link created before this shipped keeps `ALL_TIME` and renders byte-identically; the founder link builder is unchanged.
- **Updates** (`/admin/updates`) — cross-portfolio feed of every published update (drafts excluded), with search (title/company), a company filter, and three sorts (newest/oldest/company A–Z); each row links to the update detail. Fetch-once + client-side filtering, no pagination — reuses the existing `GET /api/admin/updates` endpoint that already powered the link builder (shipped 2026-07-03)
- **Weekly Digest** (`/admin/digest`) — compose the internal team digest (6 fixed sections + todo list with assignees); AI-assisted drafting via Anthropic Claude from pasted meeting notes or Granola transcript links; sent by email to recipients configured in Settings
- **Company Notes** — admin-only internal notes on each company, with revision history
- **Service Provider Directory** (`/providers`, `/admin/providers`) — founders submit and endorse service providers; admins vet submissions (pending/vetted/rejected), manage categories, and add providers directly (`POST /api/admin/providers`, defaults to Vetted per D2, audit-logged as `PROVIDER_CREATED`)
  - **Real vetting restored (2026-07-03, WS7)**: founder submissions land as PENDING and stay there until an admin promotes or rejects them; endorsements (self or peer) are now pure testimonials and never change status. Previously submissions were auto-promoted to VETTED by the submitter's own endorsement, and any endorsement promoted a pending provider — so the admin queue never filled and "Vetted" didn't mean admin-reviewed. Pending submissions remain visible in the founder directory's community tier by design (a deliberate, deferred decision, not a bug).
- **Company member management** — add members by email from company detail page; new users created automatically via invite flow; membership role dropdown (Owner/Editor/Viewer) per founder member
- **Audit Log** (`/admin/audit`) — timestamped record of every admin mutation (approvals, deletions, provider/digest/note/sector/template CRUD, member adds, manual reminders, test emails); last 100 shown; write-only from `src/lib/audit.ts`, never blocks the action it logs (shipped 2026-07-03)
- **Pre-Investment Due Diligence Intake** (`/admin/diligence`, shipped 2026-08-03, Part 16, WS38–WS43) — admin-initiated invite flow for a company that isn't a portfolio company yet: from `/admin/companies/new`, ticking "This is a due-diligence intake" (plus an optional Stellar-ecosystem checkbox) creates `Company{stage: "DILIGENCE"}` and a `CompanyDiligence` row, invites the founder with DD-specific email copy, and routes to `/admin/diligence` instead of the company page (a DD company doesn't appear on `/admin/companies` until promoted). `/admin/diligence` splits DD companies into "Awaiting founder" / "Ready for review" (completion recomputed live on every load) with an explicit **Promote** action (`stage → "ACTIVE"`, audit-logged `COMPANY_DILIGENCE_PROMOTED`; deliberately never touches `Fund`/`Deal`/`PortfolioCompany` — a real deal-ledger entry stays a separate, later, manual step) and a **Decline** action that deletes the company and — best-effort — the founder's account if they have no other company membership (see F35 below for a known edge case). Also repaired in the same batch: **`/admin/companies/new`'s "Assign User" flow was silently broken** (F32) — the client posted field names the API never read, so "assigning" a founder to a new company always made the *admin* the owner instead; fixed for the ordinary (non-DD) path too, with a dedicated regression test. And: **`isInternal` is now real access control, not just a display badge** (F33) — `GET /api/companies/[id]/documents` filters internal documents out for non-admins, and `GET /api/documents/[id]` 403s a non-admin reading one, even the founder who uploaded it. Full plan, findings (F30–F35), decisions (Q53–Q60), and judgment calls in Part 16 of `docs/IMPLEMENTATION_PLAN.md`.
- **Funds / LPs / Deals** (`/admin/funds`, `/admin/lps`, shipped 2026-07-08, WS16) — foundation for the LP Fund-Report Portal (Part 7 of `docs/IMPLEMENTATION_PLAN.md`): admin CRUD for funds (name/group label/AUM/first-deal-date), portfolio companies (with an editable, exact-match-suggested link to an operational `Company` row, Q14), and per-fund deals (initial/follow-on, amount, instrument, entry/current valuation — editing `currentValuation` stamps `valuationAsOf`). A one-time importer (`scripts/import-investment-tracker.ts`, dry-run by default) populated the real dataset from DFS Lab's investment tracker spreadsheet: 76 deals across 50 portfolio companies and 7 funds, idempotent on re-run. **Annotated 2026-07-15 (Part 10, Q22-B):** a sheet-backed transition is planned — WS27 adds an env-gated one-way Google-Sheets sync under which sheet-owned deal/valuation fields become read-only here while sync is enabled; see the "Next up" note under Roadmap below.
- **Fund Report authoring** (`/admin/reports`, shipped 2026-07-09, WS17) — admins write letter-style LP reports per fund in a mention-aware editor built on the WS20 composer shell; typing `@` opens a picker (new `@tiptap/extension-mention` dep, pinned to `3.19.0` to match the rest of the TipTap install) scoped to portfolio companies with a deal in that fund (mentioning a company the fund never invested in is refused at publish time). Publishing **freezes valuation snapshots** — per Q6 (decided 2026-07-08), the hover card headline is the SINCE-FIRST-CHECK multiple (earliest deal's entry → current valuation), not a blended figure, with a full per-deal breakdown (DFS check sizes, multiples, and dollar valuations) beneath. Lifecycle is unpublish-to-edit (Q13): a published report is read-only until "Unpublish to edit," and republishing re-freezes fresh numbers. A draft preview page renders the exact same `ReportView`/hover-card components the LP page (below) uses, computed live (never frozen) so the preview is honest. **Fund Performance Snapshot shipped 2026-07-29 (Part 14, WS33–WS36, Q40–Q45):** admins can insert a new, always-visible "fund performance snapshot" block at the cursor (Q45-A — a new TipTap block node, not the hover-mention pattern) showing the same five Performance stats (Invested/Implied Value/TVPI/DPI/Gross IRR) and full per-deal table (Q41-A) as the admin fund page — a narrow, explicit carve-out of the Q23 admin-only wall (Q40-A) scoped to that report's own fund only, never a fund picker (Q43). No "synced from sheet" provenance ever reaches the payload (Q42, held structurally — the type has no such field). Freezes on publish and re-freezes on every republish, exactly like `@company` mentions (Q44-A, same delete-and-recreate semantics, independent freeze row). Shared `<FundPerformanceCard>`/`<FundSnapshotBlock>` components mean the admin fund page, the admin report preview, and the real LP page all render identical markup. **Per-fund manual performance override shipped 2026-07-30 (Part 15, WS37, decisions Q46–Q52):** three admin-editable fields (Gross MOIC / Net TVPI / Net DPI) on `Fund`, generic to every fund but populated for CAF1 only for now, that replace the TVPI/DPI slots and hide Gross IRR on that fund's Performance card (everywhere it renders — admin fund page, admin report preview, and the real LP page) whenever at least one is set (JC-B: partial entry already counts). Invested and Implied Value stay Molly-computed either way. No sheet-sync involvement — manual entry only, edited via a small admin-only affordance on `/admin/funds/[id]` beneath the Performance card (never inside the shared `FundPerformanceCard`/`FundSnapshotBlock` components themselves, since that same tree is portalled into the read-only LP page), and reversible by clearing the fields. `computeFundPerformance()` itself is untouched — the override is a sibling `performanceOverride` field threaded through `buildFundReportSnapshot()`'s new optional 4th parameter, not mixed into the pure metrics engine. Every fund without override values renders byte-identical to before this shipped. Full plan, findings (F26–F29), and judgment calls in Part 15 of `docs/IMPLEMENTATION_PLAN.md`.
- **LP portal** (`/lp`, shipped 2026-07-09, WS18–WS19) — the LP-facing half of the Fund-Report Portal: limited partners authenticate at `/lp` with a one-time 6-digit email code (10-minute validity, fail-closed attempt cap independent of the Postgres rate limiter — see JC4/JC7 in Part 7 of `docs/IMPLEMENTATION_PLAN.md`) and land on a ~30-day session (a separate `lp_session` cookie, not a NextAuth audience), then see a library of every published report across their funds and report pages with the frozen hover cards. `/lp` and `/api/lp` are public at the routing layer by design — LPs have no NextAuth session — with the real gate being the cookie check inside Server Components that read the DB directly, the fifth instance of this codebase's "sessionless client" pattern (after `/api/cron`, `/brand`, `/api/share`, inline images). Admins get an opt-in "Notify this fund's LPs by email" checkbox on publish (template #12, best-effort per recipient) and LPs get a `@media print` stylesheet as the paper escape hatch (Q11) — no PDF generation. Zero new env vars, zero new paid services. **Idle timeout added 2026-07-20 (Part 10, WS23, Q21/JC14):** the 30-day persistent session stays, but a session idle more than 7 days (`LpSession.lastUsedAt`) is now treated as logged-out even before the hard 30-day expiry — an LP who returns after a quiet stretch just re-verifies with a fresh OTP. **Contrast bug fixed 2026-08-04:** every LP-facing file (`/lp`, `/lp/reports/[id]`, the login form, the layout footer) had used `text-muted` instead of `text-muted-foreground` — `text-muted`'s `DEFAULT` resolves to the Bone background/border color, not a text color, so fund labels, period labels, dates, the footer wordmark, and the login form's placeholders/helper link were all barely legible against the Paper canvas. Found live via a real screenshot, fixed in all four files at once rather than just the one reported. **Verified in Part 22's audit:** an app-wide grep for the same mistake found no other instance anywhere else in the codebase — this was scoped to the LP portal's own original build, not a repeated habit elsewhere. **Optional per-publish note shipped 2026-08-05 (Part 24, WS54, F47):** the publish-confirm dialog now shows an optional, 500-character-capped free-text note textarea alongside the notify checkbox (visible only when the checkbox is checked); when filled in, it renders as its own escaped paragraph in the report-published email after the "We've just published…" intro and before the "Read the Report" button, with author line breaks preserved, and is captured (not persisted elsewhere) in the existing `REPORT_PUBLISHED` audit metadata as `noteIncluded`/`note`. Left blank (the default), the email is unchanged except that "It's a short read." is permanently gone from the intro paragraph. Also fixed in the same edit (F47): `reportTitle` and the LP's first name were interpolated into the email HTML unescaped — a title or name containing `<`, `>`, `&`, or `"` now renders as literal text instead of breaking the markup. **Multiple email addresses per LP shipped 2026-08-06 (Part 26, WS58–WS61, F51):** an LP can now hold several addresses (`LpEmail`, globally unique) instead of admins creating duplicate LP rows for a personal+work address or a shared fund inbox. Any of an LP's addresses can request/verify the OTP and land in the same account with the same fund access (D1); the opt-in publish notification now reaches every address, never double-mailing the same fund (D2 — verified structurally dedup-free per F51: a publish is single-fund and `@@unique([lpId, fundId])` visits each LP once); and removing an address only signs the LP out if it was their last one (D3), replacing the old "any email edit revokes every session" behavior. Admins manage addresses (add/remove/set-primary) from a new list in the `/admin/lps` edit modal, audit-logged as `LP_EMAIL_ADDED`/`_REMOVED`/`_PRIMARY_CHANGED`. `LimitedPartner.email` remains as a nullable, synced mirror of the primary address for any code that wants a quick scalar; the real uniqueness guarantee now lives on `LpEmail.email`.
- **Report editor draft-integrity polish** (shipped 2026-07-20, Part 10, WS23.2/23.3) — the publish-time "mentioned company has no deal in this fund" error now names a deleted company honestly ("a company that no longer exists") instead of printing its raw id (F24); the report editor also shows a live "Mentioned companies" chip strip under the `@`-mention hint, so a stale/deleted mention shows a red warning chip before the admin ever hits Publish.
- **Portfolio ledger schema + reversible backfill** (shipped 2026-07-20, Part 10, WS24) — additive `FinancingRound`/`ValuationMark`/`FundCashflow` tables plus nullable `Deal.roundId`/`convertedInRoundId`/`ownershipPct` columns lay the foundation for round-level history, dilution-aware valuation, and scripted IRR/TVPI (JC16–JC18). `scripts/backfill-rounds.ts` (dry-run by default, `--apply`, `--revert`) synthesized one `FinancingRound` per existing deal and one `ValuationMark` per portfolio company from Molly's own data (JC17) and has already been run against production: 76 `FinancingRound` rows created (one per deal, matching the verified 76-deal/50-company count — not the stray 78/52 that was investigated and corrected before this ran), 50 `ValuationMark` rows created, zero valuation disagreements, re-run confirmed fully idempotent ("0 to do"). Invisible in this workstream — no UI reads the new tables yet; that's WS25.
- **Cross-fund portfolio views + ledger CRUD** (`/admin/portfolio`, shipped 2026-07-20, Part 10, WS25) — the F25 gap closes: an all-deals view spanning every fund (summary stat cards, fund/type filters, company + instrument search) and a per-company cross-fund page (`/admin/portfolio/[id]`) showing deals across every fund the company has taken money from, a computed per-fund "position" block (invested, deal count, latest multiple — JC18, never persisted), a financing-rounds timeline (create/edit/delete), and valuation-mark history with a "record new mark" form that fans out the new valuation to every one of that company's deals in one transaction (JC16 — the same effect as today's per-deal inline edit, so published report hover cards stay byte-identical). The existing per-fund deal PATCH additively accepts `roundId`/`convertedInRoundId`/`ownershipPct` (inline round-assignment `<select>` + ownership input on the company page). `/admin/funds/[id]` gained a Cashflows tab (capital calls/distributions/fees, Pattern B wrap-row cards) with a placeholder note for WS26's performance card. Admin-only; no founder/LP/investor surface changed. **Density/navigation-cue polish shipped 2026-07-21 (Part 13, WS30–WS31):** sticky header, row hover, wider cell padding, client-side sortable columns (Company/Fund/Date/Amount/Current Val./Multiple/Position Value), and removal of the Round column (confirmed 100% duplicate of Instrument in production data) via a new shared `Table`/`TableHead`/`Th`/`SortableTh`/`TableRow` primitive (`src/components/ui/table.tsx`), also swept onto `admin/portfolio/[id]`, `admin/lps`, and `admin/funds/[id]`.
- **Derived portfolio metrics — admin-only** (shipped 2026-07-20, Part 10, WS26) — `src/lib/portfolio-metrics.ts` (pure, unit-tested, 17 tests) computes gross IRR (Newton's method with a bisection fallback, guard-railed to a -99.99%–1000% range, `null` on non-convergence), TVPI/DPI (with an explicit `≈` badge whenever a fund has no recorded `CAPITAL_CALL` rows and paid-in capital falls back to total invested), and dilution-aware position values (`ownershipPct × latest valuation` when known, else the existing zero-dilution "amount × multiple" assumption with a small dot-badge marking it as such). Surfaced on `/admin/funds/[id]` (new Performance card: invested, implied value, TVPI, DPI, gross IRR, as-of date, with an honest "admin-only estimate; gross of fees unless FEE rows are recorded" footnote), `/admin/portfolio` (blended implied-value stat card + a per-deal Position Value column), and `/admin/portfolio/[id]` (per-fund-position implied value). **Q23: admin-only in v1 — nothing on `/lp`, the share pages, or hover-card/snapshot rendering changed** (grep-verified: `portfolio-metrics` is imported only under `src/app/api/admin/**`).
- **Sidebar navigation regrouped** (shipped 2026-07-20, Part 11, WS28) — after five parts of feature growth, the admin nav was a flat 13–14-item list with no grouping; it's now three labeled groups — "Company Operations" (Approvals, Companies, Updates, Update Templates, Investor Links), "Funds & LPs" (Funds, Deal Ledger, LPs, Fund Reports), "Admin Tools" (Weekly Digest, Service Providers, Audit Log, Settings) — plus an ungrouped Dashboard at top, using the app's existing mono-uppercase-tracking label convention for group headers. "Portfolio" is renamed "Deal Ledger" and "Templates" is renamed "Update Templates" (copy-only, hrefs unchanged); "Fund Reports" (`/admin/reports`) is linked from the sidebar for the first time. Founder nav is reordered to frequency: Dashboard, Updates, Metrics, Investor Links, Company Profile, Team, Service Providers. **"Sync" no longer appears anywhere in the sidebar** — confirmed from the data model (`SheetSyncRun` has no `fundId` column; `sheetsSyncEnabled()` takes no fund argument) that sync is a genuinely global integration, not fund-scoped, so rather than add it as a nav item or duplicate it identically across every fund's detail page, its status/history/link-matching UI (`src/components/admin/sync-panel.tsx`) now lives as a single "Sync" tab on the `/admin/funds` list page, gated the same way the old nav item was (hidden entirely when Sheets sync isn't configured). `/admin/sync` is now a redirect to `/admin/funds?tab=sync` for old links. **Visual/accessibility polish shipped 2026-07-20 (Part 12, WS29):** group headers gained `font-semibold` (matching the app's `.label` convention) plus a `border-t border-border` divider between groups, `role="group"`/`aria-labelledby`/`aria-current="page"` semantic markup, a wider Dashboard-to-first-group gap, and an active-group header highlight (`text-foreground` vs `text-muted-foreground`); `Deal Ledger`'s `PieChart` icon became `Rows3` and `Service Providers`' `Briefcase` became `Wrench` in both the founder and admin nav.

> **Note on AI Chat:** the OpenAI-based RAG chatbot (and pgvector embeddings) was deliberately removed, pending a cleaner re-implementation. Re-introduction is planned — see P3 below. The dead `openai` dependency was dropped from package.json on 2026-07-03 along with five other unused packages (`ai`, `uuid`, `@types/uuid`; `diff`/`@types/diff` were initially flagged too but turned out to be in active use by the company-notes diff view and were restored).

### Document Management
- Upload documents linked to a company or specific update
- Document types: Pitch Deck, Financials, Legal, Product / Demo, Other
- Filter by type, search by name, toggle archived documents
- Archive / unarchive without permanent deletion
- Internal-only flag for admin-visible documents
- Document type badge shown on update detail attachments
- **Server-side MIME/extension allowlist** (as of 2026-07-03) — the upload endpoint no longer trusts the client-supplied MIME type; PDFs, Office formats, images, CSV/TXT/ZIP, and MP4/MOV are allowed and must match a real file extension
- **Inline document viewing shipped 2026-08-04 (Part 19, WS45)** — a "View" action (PDF/image, opens inline in a new tab) now sits beside Download on both the admin company page and the previously-inert update-detail Attachments list. Also closed in the same pass: `GET /api/documents/[id]/view` had no `isInternal` access check (F37), and the update-detail Attachments API leaked `isInternal` documents to non-admins (F38) — both fixed. See "Inline Document Viewing" below (Part 19, WS45 of `docs/IMPLEMENTATION_PLAN.md`).
- **Founder document upload/browse surface shipped 2026-08-04 (Part 20, WS46–WS47)** — `/company/documents` gives founders a general document library (upload/view/download, all 10 `DOC_TYPES`), independent of `/diligence`'s DD-specific 5-slot checklist which self-disables once a company is promoted to `ACTIVE`. Also closed in the same pass: `PATCH /api/documents/[id]` (archive/retype) previously had no admin or ownership check at all — any founder member of a company could call it directly to archive or retype any document, including `isInternal`-flagged ones (F39); now `requireAdmin()`-gated. See the "Company Documents" bullet under Founder Features above.

### Design & Branding
- **DFS brand system applied app-wide** — Paper/Bone/Obsidian/Sky palette, Space Grotesk / IBM Plex Sans / JetBrains Mono typography, flat/structured corners (no pill/rounded shapes) instead of the previous generic SaaS theme
- **Dark mode removed** — light theme only; the old teal-accented dark theme and its toggle were fully deleted, not just hidden
- **`_Molly` wordmark** — the placeholder auto-generated logo image is gone; a reusable text mark (mono font, brand-colored underscore) is used across the sidebar, nav bars, and auth pages
- Real DFS logo (pulled from DFS Lab's brand assets, not a placeholder) used in transactional email headers
- Status colors (badges, alerts) remapped from generic Tailwind red/green/amber to the brand's own Laterite/Acacia/Ochre
- **Mobile-width layout hardening** (shipped 2026-07-07, WS14–WS15) — app-wide layout correctness at phone widths (~375px): scrollable tables with explicit min-widths, wrap-row cards (link cards, members, approvals, notes, digest list), a scrollable company-detail tab strip, wrapping metric/setup-wizard form rows, stacked publish/schedule banners, stacked provider-modal field grids, and a stacked (below `sm:`) notes revision diff modal. Every fix is className-only and inert at ≥640px. Includes one breaker found via a real device screenshot mid-batch, outside the original audit: the founder `/providers` card grid had no base `grid-cols-1`, so the browser used an unconstrained implicit grid track that let each card blow out past its container, pushing the status badge and Endorse button off-screen — confirmed and fixed by reproducing the exact markup in headless Chrome at a true 375px viewport. Full detail in Part 6 of `docs/IMPLEMENTATION_PLAN.md`.

---

## Roadmap

Priorities run P0 (do first) through P3 (later).

> **Confidentiality audit of the public repo (Part 27, 2026-08-06) — findings recorded, remediation is a pending Joseph decision.** Full detail (by file:line + category, never restating the sensitive content) in Part 27 of [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md), F52–F57. **No secrets or credentials leaked anywhere in the working tree or full git history — no credential rotation is required**; `.env*`/`.claude/` are correctly gitignored and the financial-data importers read real figures only from an external xlsx path. The findings are about *identifying/operational* information sitting in **public git history** (so forward edits do not remove them): a named portfolio company tied to a real personal-data DD-document incident (F52, HIGH), ~15 real portfolio-company names with deal detail incl. several named as written off (F53, HIGH), DFS's real fund-vehicle labels in code/README/docs (F54), a named team member + a fund's internal reporting request (F55), the real vetted-vendor directory in `prisma/seed.ts` (F56), and real company names as a UI placeholder (F57). Remediation is Joseph's call between rewriting history (`git filter-repo` + force-push), making the repo private, or accepting — Felix does not perform any of these. Optional, separate fork-hygiene code edits (genericize the vehicle constant, seed data, and placeholder) are Alvin tasks outside Felix's docs-only scope.
>
> **Optional per-publish note on the LP report-published email shipped 2026-08-05 — Part 24, WS54 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F47).** Adds an **optional** free-text note to the LP report-publish flow, settled with Joseph via an approved mockup before any code was written. When filled in, the note renders as its own escaped paragraph in the report-published email (`sendLpReportPublishedEmail`) — after the "We've just published…" paragraph, before the "Read the Report" button, author line breaks preserved; when left blank (the default), the email is unchanged except for the also-shipped, unconditional removal of "It's a short read." from that paragraph. The note is transient/per-publish, captured only in the existing `REPORT_PUBLISHED` audit metadata (`noteIncluded` + verbatim `note`, no schema change, no persistence elsewhere), capped at 500 characters client- and server-side, and shown in the publish-confirm dialog only when the notify checkbox is checked. **F47** (found during planning verification, fixed in the same edit): `sendLpReportPublishedEmail` interpolated admin-authored `reportTitle` and the LP first name into the email HTML unescaped — a title/name containing `<`, `>`, `&`, or `"` would have broken the markup; now escaped via a new module-private `escapeHtml` helper mirroring `src/lib/pdf.ts`. No new dependency, no new env var, no new cost line, no schema change. Shipped feature detail folded into the LP portal bullet under "Existing Features" above.
>
> **Planned — Part 23 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F42–F46, WS49–WS53), scoped 2026-08-04, not yet built.** Turns the five findings from the Part 22 audit below into a full implementation plan, per Joseph's request. **Both open product decisions confirmed by Joseph 2026-08-04, each matching Felix's recommendation exactly — the plan is now final, but Alvin has not been engaged and implementation has not been green-lit:** **Q67** — orphaned-`Document`-row cleanup is a read-only scan plus an admin-reviewed list, with a guarded per-row delete (re-verified server-side, audit-logged) rather than automatic cleanup, matching the guarded-delete precedent Part 21/Q66 already established; scan-only for v1, no write-path "confirm" signal. **Q68** — the new storage-diagnostics tooling folds into the existing `/admin/settings` page, beside the existing Email section, rather than getting its own page. Planned workstreams: **WS49** — a "Send Test Upload" diagnostic on `/admin/settings` mirroring the existing "Send Test Email" button (F43); **WS50** — an on-demand orphaned-document scan-and-review tool (F42), plus fixing the update composer's inline-image upload, which was found during re-verification to insert a broken image reference with no error at all when its `PUT` fails (same root cause as F42, not a new finding); **WS51** — audit-logging `PATCH /api/documents/[id]` (F45); **WS52** — deduplicating the `EMAIL_FROM` fallback string (F44); **WS53** — the exact `SETUP.md` CORS-step text for a human or differently-scoped agent to apply (F46, outside this agent's edit scope). No schema changes, no new dependencies, no new cost line. Full findings recap, confirmed decisions, and file-by-file code sketches in Part 23 of `docs/IMPLEMENTATION_PLAN.md`.
>
> **Second platform audit — Part 22 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F42–F46), 2026-08-04.** A follow-up to the 2026-08-03 audit below, prompted by a fast-moving session that shipped Parts 18–21 (WS44–WS48) plus a batch of same-day, non-workstream commits: a real production incident (R2 storage credentials invalid for 167 days, plus a separately-diagnosed missing CORS policy on the replacement bucket, both now fixed outside the repo — no code change was needed), a founder-own-`isInternal`-doc visibility fix (folded into the Company Documents bullet above), a branding pass (`ORG_NAME` default, email sender name, LP email copy — folded into Fork Configuration above), and an app-wide `text-muted`→`text-muted-foreground` contrast fix that had been repeated four times within the LP portal's own original build (folded into the LP portal bullet above; a full app-wide grep confirmed the mistake exists nowhere else). Parts 18–21 were re-verified end-to-end against the working tree (not just their own plan docs) and hold up — the DD-completion-email trigger, the Part 19/20 `isInternal` access-control fixes, and Part 21's dismiss/delete queue hygiene (including its FK-safety guarantees beyond `Company.createdById`) all check out exactly as documented. Five new findings, all cheap, none blocking: **F42** — document upload is non-atomic (the `Document` DB row is created before the client's S3/R2 `PUT` completes, with no confirmation step or reconciliation), which is the actual root cause of the 16 "phantom" rows manually deleted this session and will recur on any transient upload failure, not just a credential outage; **F43** — there is no credential/CORS health-check for storage, unlike the existing "Send Test Email" diagnostic for Resend, which is exactly the blind spot that let the R2 breakage go unnoticed for 167 days; **F44** — a second, independent copy of the `EMAIL_FROM` fallback string on `/admin/settings` went stale during the branding pass (display-only, low severity); **F45** — `PATCH /api/documents/[id]` (archive/retype), made admin-only by this session's own F39 fix, is not audit-logged despite every structurally-equivalent admin mutation route being so; **F46** — `SETUP.md`'s R2 setup steps have no CORS-configuration instructions, meaning a new fork would hit this session's exact incident (flagged, not fixed — `SETUP.md` is outside this audit's edit scope). Full detail, verification transcripts, and recommended next steps for each finding in Part 22 of `docs/IMPLEMENTATION_PLAN.md`.
>
> **Awaiting-Setup Queue Hygiene shipped 2026-08-04 — Part 21, WS48 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F40, F41).** From Joseph 2026-08-04: `/admin/approvals`'s "Awaiting password setup" section (WS22) had rows sitting there, still "link expired," since February — no way to clear them. Verified: no cleanup mechanism existed (F40 — the existing "Reject" action can't reach this population at all, and would send a real rejection email to an already-approved founder/teammate even where it could); a hard account delete is unsafe to do uniformly (F41 — a self-signup founder is the `createdById` of their own company, a required non-cascading FK, while an admin-invited founder/teammate is not, so only the latter can be deleted cleanly). **Q66 confirmed by Joseph 2026-08-04 (Felix's recommendation accepted): shipped both** — a safe, reversible, no-email "Dismiss/Undismiss" action (new `User.setupQueueDismissedAt` column, audit-logged) plus automatic stale-grouping (30+ days past a row's own token expiry, display-only), **and** a guarded hard "Delete account," offered only when F41's `canHardDeleteFromQueue()` predicate confirms the row isn't a company creator or an admin — blocked otherwise with an inline explanation. No schema change beyond the one nullable column; no new cost line. Shipped feature detail folded into "Existing Features" above.
>
> **Founder Document Upload for Active Companies shipped 2026-08-04 — Part 20, WS46–WS47 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F39).** Surfaced by a real incident: a founder's 5 due-diligence document uploads (cap table, bank statements, certificate of incorporation, business license, passport) never actually saved because of a since-fixed R2 storage bug, and once their company was promoted out of `DILIGENCE`, `/diligence` became a dead end (`return null` the instant `stage !== "DILIGENCE"`) with no other founder-facing document surface anywhere in the app. Joseph chose the broader fix over a narrow one-off, reasoning the gap will recur regardless of this specific incident: a new `/company/documents` page, always in the founder sidebar (unlike `/diligence`, deliberately not gated by company stage — DD founders already get full, non-blocking access to everything else per Part 16/Q55), reusing the existing presigned-upload flow, `GET /api/companies/[id]/documents` (already `isInternal`-safe for non-admins), and the Part 19 View/Download pattern as-is — zero new endpoints needed for reads. Also closed **F39**, found during this review and independent of any UI change: `PATCH /api/documents/[id]` (archive/retype) had no role or ownership check at all — any founder member of a company could already call it directly, with no UI needed, to archive/unarchive/retype any document belonging to their company, including `isInternal`-flagged ones like a teammate's passport; now `requireAdmin()`-gated, an unconditional endpoint-level fix confirmed by a new regression test. **Q65 confirmed by Joseph 2026-08-04 (Felix's recommendation accepted): upload/view/download only — no archive/delete UI on the founder page.** A founder who mis-uploads a document asks an admin to fix it, same as today. No schema change, no new cost line. Shipped feature detail folded into "Existing Features" above.
>
> **Inline Document Viewing shipped 2026-08-04 — Part 19, WS45 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F37, F38).** Joseph noticed every document click forces a browser Save-As download, even for PDFs/images a browser can natively render inline, and asked for a plan to add viewing alongside (not instead of) the existing download. Diagnosed and verified: the only forced-download call site was the admin company page's `handleDownload` (an unconditional HTML `download` attribute — not an S3/storage constraint; `getDownloadUrl()` sets no `Content-Disposition`), and an inline-view route (`/api/documents/[id]/view`) already existed and worked, built for the rich-text editor's embedded images. Two real gaps surfaced during verification, beyond the original diagnosis, and were closed in the same pass: **F37** — that `/view` route had no `isInternal` access-control check (the Part 16/WS42/F33 fix never reached it); **F38** — the update-detail page's "Attachments" list fetched `isInternal` from the API but never filtered on it (same gap, a route WS42 never touched), and, separately, wasn't clickable at all — no view, no download, nothing. Shipped as one workstream: a shared, tested `isInlineViewable(mimeType)` helper (PDF + PNG/JPEG/GIF/WebP only, matching Joseph's own "PDF/image at minimum" framing), a "View" icon (opens inline in a new tab) next to the existing Download icon on the admin document table, the same two actions added to the previously-inert update-detail Attachments list, and the F37/F38 `isInternal` fixes shipped alongside so this Part didn't create a new way to leak internal-only document content. No schema change, no new cost line. Confirmed founder/LP surfaces were genuinely out of scope at the time: `company/profile` had no document UI at all, `/diligence`'s checklist page shows name/date only by design, and no LP/share-facing page renders a document list of any kind — Part 20 (below) has since added a founder-facing document page separately. Shipped feature detail folded into "Existing Features" above.
>
> **Due Diligence Completion Signal shipped 2026-08-04 — Part 18, WS44 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (F36, decisions Q61–Q64, every Felix recommendation accepted).** A real founder hit the exact gap this closes: their `/diligence` checklist showed 4 of 4 required items done, but the dashboard banner still said "a few more steps," `/diligence` never distinguished a complete state (and, by the original WS40 design, has no Submit button at all — deliberately not reopened here), and no notification reached anyone — Joseph found out only because the founder emailed asking if it was a bug. Scoped as a fix, not a new feature: the dashboard banner and `/diligence`'s status line both switch to the user's confirmed copy ("All done! We are reviewing your documents and will be in touch soon.") once `completedAt` is set, the banner's button relabels "Continue" → "View," and two new transactional emails fire exactly once at the real completion moment (detected off the existing `recomputeDiligenceCompletion` null→non-null transition, suppressed for admin sessions) — a founder confirmation (since there's no Submit button, this is the only guaranteed receipt) and an admin notification to `TEAM_EMAIL` (matching the existing `sendNewSignupNotification` pattern) so `/admin/diligence`'s already-existing "Ready for review" section stops being purely pull-based. No schema change, no new cost line (Resend only). Shipped feature detail folded into "Existing Features" above.

> **Pre-Investment Due Diligence Intake shipped 2026-08-03 — Part 16, WS38–WS43 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (decisions Q53–Q60, every Felix recommendation accepted).** The first pre-investment concept in Molly: today every other domain model (`Deal`, `Fund`, `PortfolioCompany`) assumes the company is already an investee, but DFS Lab's real workflow is admin decides to invest → founders complete a due-diligence questionnaire and document upload → deal closes (or doesn't). Scoped so the DD founder gets a **real Molly account and full, non-blocking dashboard access from day one** — a new `Company.stage` (`"DILIGENCE" | "ACTIVE"`) and `CompanyDiligence` model, an admin-initiated invite flow (which also fixed a pre-existing, never-noticed bug — F32 — where `/admin/companies/new`'s "Assign User" picker silently made the *admin* the company owner regardless of what was typed in), a founder-facing checklist page reusing the existing document-upload system with 5 new document types (cap table, bank statements, certificate of incorporation, business license, founder passport), a new admin review queue (`/admin/diligence`) with an explicit promote-to-portfolio action, and a scoped fix (F33) closing a real pre-existing gap where the `isInternal` document flag was never actually enforced as access control. Deliberately does **not** touch the tested auth core (`auth.ts`/`auth-guard.ts`/`route-access.ts`/`set-password`'s status write) — the founder becomes `APPROVED` immediately, exactly like an ordinary team invite, with only the company's own `stage` distinguishing DD from a real portfolio company. **Corrected mid-flight (F34):** WS41/WS43's original plan assumed `DELETE /api/companies/[id]` already deleted the founder's account on a decline — it didn't (`Company` deletion never cascades to `User` in the schema) — so WS43 was re-scoped, with the user's explicit sign-off, to add narrowly-scoped, best-effort founder-account cleanup alongside the delete. **F35, found in this audit (2026-08-03):** that cleanup is best-effort and silently no-ops if the founder created a `ShareableLink` before being declined (a required, non-cascading FK — fully reachable, since Q55 grants Investor Links access from day one) — the admin's confirm dialog and the `DELETE` response both claim unconditional removal either way, with no signal that the account survived. See "Open findings from the 2026-08-03 audit" below. Full findings (F30–F35), decisions, judgment calls, and six workstreams (schema → invite fix → founder checklist → review queue → doc-security fix → decline-copy/account-cleanup) in Part 16 of `docs/IMPLEMENTATION_PLAN.md`. Shipped feature detail folded into "Existing Features" above.

> **Findings from the 2026-08-03 audit — both closed.** Felix reviewed Parts 14–16 end-to-end (code, not just plan docs) plus a broader pass for the two recurring bug shapes this codebase has hit before ("sessionless client hits auth middleware," "client/API field-name mismatch") — neither pattern recurred anywhere new. Two real, narrow findings surfaced, both in Part 16's DD-decline path, and both are now fixed:
> - **F34 (fixed)** — WS41/WS43's original plan assumed `DELETE /api/companies/[id]` already deleted a declined DD founder's account; it didn't, because `Company` deletion has no cascade to `User` in the schema. Caught and corrected the same session, with the user's explicit sign-off before shipping an irreversible account-deletion change (commit `6f144dd`).
> - **F35 (fixed 2026-08-04)** — the F34 fix was best-effort and silently no-op'd if the founder had created a `ShareableLink` before decline (its `createdById` is a required FK with no cascade — reachable in the ordinary course of using Investor Links, which Part 16/Q55 explicitly grants DD founders from day one). Fixed (commit `fc0f60f`): declining now also cleans up any `ShareableLink` rows the founder created that no longer reference any company (their join rows already cascaded away with the deleted company, so a zero-association link only ever pointed at the dead deal — safe to remove), and a genuinely unrecoverable case is no longer swallowed — the API response gains `founderAccountsRetained` alongside `founderAccountsDeleted`, audit-logged, and `/admin/diligence` shows an honest follow-up banner instead of a false "this cannot be undone."
> - **F31 (fixed 2026-08-04, unrelated to Part 16)** — found during the same audit pass: the Setup Wizard's document-upload step (`/setup-wizard`, predates all DD work) had no `onChange` handler at all — self-signup founders believed they'd uploaded documents and nothing happened. Fixed (commit `101113b`) by wiring it to the same presigned-upload flow WS40 built for `/diligence`.
>
> Everything else checked against Parts 14–16 — the admin-only wall grep guards, the freeze/re-freeze semantics, the override inertness guarantee, the `isInternal` enforcement (F33) and its regression tests, the F32 fix's own regression test — matched the plan docs' claims exactly, verified by reading the actual shipped code, not just the plan.

> **Per-Fund Performance Override shipped 2026-07-30 — Part 15, WS37 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (decisions Q46–Q52, all answered by the user).** From a request Stephen (DFS Lab team) sent Joseph right after the Part 14 walkthrough: CAF1's internal tracking sheet now carries Gross MOIC / Net TVPI / Net DPI, and he asked to show those (alongside Invested/Implied Value) and drop Gross IRR for that fund "for now." Investigated first, discussed second, per house convention — confirmed Molly's existing computed TVPI/DPI/Gross IRR are gross-of-fees by construction with no code path to "net" absent `FEE` cashflow rows, and CAF1 has none (no bulk/automated mechanism has ever populated `FundCashflow` for any fund), so this was a genuine data gap Stephen's numbers close, not a relabeling. Shipped as a manual admin override (Q46 — no sheet-sync automation), generic schema but CAF1-populated only (Q47), replacing the TVPI/DPI slots and hiding Gross IRR only while override values are present (Q49/Q50, JC-B: at least one non-null field counts), reversible by clearing them. Three new nullable `Fund` columns, additive `db push`. Full plan, findings (F26–F29), and judgment calls in Part 15 of `docs/IMPLEMENTATION_PLAN.md`.

> **Fund Performance Snapshot in LP Reports shipped 2026-07-29 — Part 14, WS33–WS36 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md) (decisions Q40–Q45, every one accepted as recommended).** User request: insert a snapshot of a fund's Performance card (Invested/Implied Value/TVPI/DPI/Gross IRR) and full deal table — exactly as shown on `/admin/funds/[id]` — into an LP report letter as a new, always-visible block at the cursor (Q45-A), not the existing hover-card mention pattern. A deliberate, narrow carve-out of the Q23 admin-only wall (Q40-A): those five stats become LP-visible, but only inside a report an admin explicitly builds for that fund's own LPs — nothing else that reads `portfolio-metrics.ts` changed. Full per-deal detail ships too (Q41-A: company/type/date/amount/instrument/entry/current valuation/multiple, plus the "as of" valuation date per JC-D), with no "synced from sheet" provenance ever reaching LPs (Q42 — the payload type structurally has no such field) and no fund-picker needed since a report is always scoped to exactly one fund (Q43). Republishing always re-freezes fresh numbers, matching the existing `@company` mention behavior exactly (Q44-A — independent delete-and-recreate row, same as mentions). WS33 extracted the admin fund route's inline Performance math into `computeFundPerformance()`/`buildFundReportSnapshot()` in `portfolio-metrics.ts` (byte-identical-output refactor) plus additive schema (`FundReportFundSnapshot`, one row per report); WS34 added a new TipTap block node (`fundSnapshot`, static "(updates when published)" placeholder while drafting — no in-editor NodeView, per this codebase's existing renderHTML-only precedent) and one toolbar button, gated behind a `fundSnapshot` prop on `RichEditor` that only the report editor passes; WS35 wired the publish-route freeze (always keyed off `report.fundId`, never a client-suppliable id) and the draft-preview live compute; WS36 extracted `<FundPerformanceCard>` (admin fund page renders it unchanged) and added `<FundSnapshotBlock>` so the admin preview and the real LP page render identical markup. Grep-guarded throughout: the two builder functions are never imported from `src/app/lp/**` or `src/api/share/**` — those surfaces only ever read the already-frozen snapshot row scoped to their own report. Full plan, decisions, findings, and judgment calls in Part 14 of `docs/IMPLEMENTATION_PLAN.md`.

> **Deal Ledger table density & navigation cues, Select/table-shell sweep, and app-wide list-spacing widen shipped 2026-07-21 — Part 13, WS30–WS32 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md).** Third item from the same round of annotated live-screenshot feedback that produced the Update Templates move and the `Select`/card-gap fixes (below): the Deal Ledger table (`/admin/portfolio`) read as cramped with no navigation cues. Verified against live production data, not just the screenshot: the Ownership column is 100% empty (76/76, by design — sparse-fill-in-over-time field, not a bug), but the Round column was confirmed **100% duplicate of Instrument** (76/76 byte-identical) — the basis for **Q37-A**, shipped: dropped from the ledger (11→10 columns; the full `FinancingRound` editor is untouched on the per-company drill-down). **Q38-A**, shipped: client-side sortable columns (Company/Fund/Date/Amount/Current Val./Multiple/Position Value), default sort unchanged at `dealDate` desc. New shared `src/components/ui/table.tsx` primitive (`Table`/`TableHead`/`Th`/`SortableTh`/`TableRow`) also picked up a sticky header, row hover (reusing the app's `hover:bg-muted/50` card idiom), and a cell-padding bump — then swept (WS31) onto the three other admin tables confirmed to share the Deal Ledger's exact pre-existing shell (`admin/portfolio/[id]`, `admin/lps`, `admin/funds/[id]`), and the `Select` component's rollout was finished across all remaining native `<select>` sites app-wide (two left deliberately native — a chromeless composer dropdown and a dense inline table-cell select — see the implementation report for why). **Q39-A**, shipped (diverging from the plan's own "leave scoped" recommendation): `space-y-3` → `space-y-4` (12px→16px) widened at every confirmed list-item site app-wide (Funds, LPs, Reports, Providers, Templates panel, Digest, Approvals, Investor Links), explicitly skipping form-field sites using the same class. Full findings, corrections to the original brief, and all three workstreams' acceptance checklists are in Part 13 of `docs/IMPLEMENTATION_PLAN.md`.

> **Move Update Templates under Updates; fix cramped selects and card gap — shipped 2026-07-21.** Three items of direct user feedback on live admin screenshots: Update Templates (previously its own sidebar item) is a sub-feature of Updates, not a peer destination — extracted into `src/components/admin/templates-panel.tsx` and mounted as a "Templates" tab on `/admin/updates` (same extraction pattern as Sync→Funds in Part 11/WS28); `/admin/templates` now redirects. A new shared `src/components/ui/select.tsx` (`appearance-none` + one explicit `ChevronDown`, modeled on `Input`) fixes every native `<select>`'s dropdown arrow rendering flush against its border — applied to `/admin/updates`'s two selects first, with the full app-wide rollout shipped in Part 13/WS31 above. That page's card-list gap also widened `space-y-3` → `space-y-4` (12px → 16px) per the "cards too close together" annotation, later widened app-wide at all confirmed list-item sites in Part 13/WS32 above.

> **Sidebar visual hierarchy, accessibility & icon polish shipped 2026-07-20 — Part 12, WS29 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md).** A follow-on design review of the WS28 grouped sidebar (below) found the three group headers, while structurally correct, rendered lighter-weight than the app's own `.label` convention with no divider line, had no ARIA grouping semantics, gave no visual reinforcement for "which section am I in," and two icon pairs (`PieChart` for Deal Ledger, `Handshake`/`Briefcase` for LPs/Service Providers) were hard to tell apart at 16px. All fixed: group headers are now `font-semibold` with a `border-t border-border` divider between groups, `role="group"`/`aria-labelledby`/`aria-current="page"` semantic markup was added, the Dashboard-to-first-group gap widened, and the active group's header now renders `text-foreground` instead of `text-muted-foreground` (Q34-A icon swaps: Deal Ledger `PieChart`→`Rows3`, Service Providers `Briefcase`→`Wrench` in both nav lists). A bigger proposal — auto-collapsing nav groups with the active group expanded and state persisted in `localStorage` — was evaluated and **deferred (Q35-B)**: the nav (15 items) still fits without scrolling on a typical desktop viewport, there's no existing collapsible-nav precedent in the codebase to build on, and the auto-expand-vs-manually-collapsed interaction has a genuine open UX question not worth resolving before it's needed. Revisit trigger: nav crosses ~20 items, a fourth group is added, or scrolling becomes an actual user complaint. The cmd+K command-palette idea (Q36-A) was preserved as a "someday" note — see P3 below.

> **Sidebar navigation IA rework shipped 2026-07-20 — Part 11, WS28 in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md).** User-requested review of the admin/founder sidebars after five parts of unrelated feature growth left the admin nav a 13–14-item flat list with no grouping. The admin nav is now three labeled groups — "Company Operations" / "Funds & LPs" / "Admin Tools" — plus an ungrouped Dashboard at top (Q28-A); "Portfolio" is renamed "Deal Ledger" (Q29-B) and "Templates" is renamed "Update Templates" (Q32-B), both copy-only; a "Fund Reports" entry now links `/admin/reports` from the sidebar for the first time (Q31-A, closing the finding that it had zero sidebar presence); founder nav is reordered to frequency (Dashboard, Updates, Metrics, Investor Links, Company Profile, Team, Service Providers — Q33-B). **Q30 diverged from the plan's original sketch:** rather than keep "Sync" as a grouped nav item, it was removed from the sidebar entirely (Q30-B) and relocated as a "Sync" tab on `/admin/funds` — confirmed from `SheetSyncRun` (no `fundId` column) and `sheetsSyncEnabled()` (takes no fund argument) that sync is a genuinely global, one-spreadsheet-feeds-every-fund integration, so it lives once on the funds list page rather than duplicated identically across every fund's detail page. `/admin/sync` now redirects to `/admin/funds?tab=sync` for old bookmarks. Full detail folded into "Existing Features" above.

> **Part 10 (WS23–WS26) shipped 2026-07-20; WS27 (Sheets sync) code-complete the same day, including the one-time deal-linking step — sync *apply* stays deliberately disabled until that linking step is actually run live.** Portfolio Ledger, Cross-Fund Views, Derived Metrics & Sheet Ingestion (Part 10 of [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md), WS23–WS27; planned 2026-07-15 from the user's LP-platform/portfolio-tracker PRD; decisions Q21–Q27 all answered 2026-07-15). WS23's LP idle timeout, WS24's ledger schema + reversible backfill, WS25's cross-fund views/CRUD, and WS26's admin-only IRR/TVPI/DPI are all live — see their entries above. **WS27 (one-way Google-Sheets→Molly sync):** the schema (`Deal.sheetRowId`, `SheetSyncRun`) went live with explicit user authorization, and every remaining piece shipped on top — the sync engine (`src/lib/sheet-sync-runner.ts`), the weekly cron (`/api/cron/sheets-sync`), the manual admin route + `/admin/sync` UI (relocated to a "Sync" tab on `/admin/funds` in Part 11, WS28 — see below), read-only enforcement on synced deal fields, and the publish-confirm staleness banner (Q27). A live-verification `curl` against the cron route ran a real apply before the one-time `sheetRowId` linking step existed, creating 27 duplicate `Deal` rows (no companies or valuations were affected); **those 27 rows have since been deleted with explicit user confirmation, and production is back to its pre-incident 76 deals.** The linking step itself — matching each deal to its sheet row by company/vehicle/investment-type/date and writing `sheetRowId` — is now shipped as an admin UI feature (the Sync tab's "Link existing deals to sheet rows" card, backed by `requireAdmin`-gated `POST /api/admin/sheets-sync/link`), not a script: `scripts/link-sheet-rows.ts` turned out to be **unrunnable locally** (Vercel provisions the Google service-account credentials as write-only "sensitive" env vars `vercel env pull` cannot retrieve) and has been deleted now that its matching logic lives in `src/lib/sheet-link.ts`. The feature previews (read-only) and applies all-or-nothing (refuses to write anything if any deal is ambiguous/unmatched), and is idempotent. It has not yet been run against production — that's the next user click-through. Only after a clean run should a human separately decide to set `SHEETS_SYNC_APPLY_ENABLED=true` and re-enable the recurring sync's real applies. See Part 10's WS27 section for the full status note.

> **LP Fund-Report Portal shipped 2026-07-08/09 (WS16–WS19, all four workstreams).** Web-native replacement for the twice-yearly PDF fund reports to LPs: admin-managed funds/deals/valuations (one-time spreadsheet import, Molly becomes source of truth), letter-style fund reports authored in TipTap with an explicit portfolio-company mention picker, valuation markups **frozen at publication** into per-mention snapshots, and an LP surface (`/lp`, styled after www.dfs.vc) with email-OTP login, ~30-day sessions, an opt-in publish-notification email, and a print stylesheet. Full plan and live-verification transcripts in Part 7 of [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md); all decisions (Q6–Q14) were answered 2026-07-08. Confidentiality ground rule held throughout: valuation/LP data never entered this (public) repo — the importer reads the tracker xlsx from a local path only, and the real end-to-end LP walk (OTP email arrival, library, report page, hover cards, print) is the pending user click-through. Shipped feature detail folded into "Existing Features" above.

> **Bug-fix batch shipped 2026-07-09 — Expired Set-Password Link Recovery (Part 9, WS21–WS22).** A real founder hit the dead end: setup links expired after 48h with no self-serve re-request and no admin resend (stuck accounts were invisible to admins — see the corrected line under Authentication & Access above). WS21 shipped an expired-state "email me a fresh link" flow on `/set-password` (token-keyed, neutral-response, rate-limited), extended the token TTL to 7 days via a single shared helper, and closed a reject-after-approve token hole (F20); WS22 shipped an admin "Awaiting password setup" queue with audit-logged resend. Full plan in Part 9 of [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md).

> **P0 and P1 shipped 2026-07-03.** All items from the 2026-07-03 roadmap review (workstreams WS0–WS5, plus a critical follow-on fix — see below) are live on production and verified end-to-end. Full step-by-step detail and verification notes are in [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md). Shipped feature detail has been folded into "Existing Features" above; summary:
> - **WS0** — reminder cron GET/POST fix, ESLint repair, six dead deps removed, legacy share-link guard, dev bootstrap hardened
> - **WS1** — Postgres-backed rate limiting on signup/set-password, MIME/extension allowlist on uploads, share-link email validation, admin audit log
> - **WS2** — middleware routing extracted into a tested pure function (`src/lib/route-access.ts`), Vitest suite with a regression test for the Feb–Jul auth bypass, free GitHub Actions CI (not gating deploys)
> - **Unplanned fix, found during WS2 live verification** — `/api/cron/reminders` was also being blocked by auth middleware itself (cron requests carry no session), independent of the WS0 GET/POST bug. Both are now fixed and confirmed live.
> - **WS3** — Update Templates (admin CRUD + founder picker)
> - **WS4** — `/investors` page replaces the broken "Investor Login" OAuth button
> - **WS5** — investor engagement signal on the founder dashboard
>
> Test coverage remains narrow (middleware routing, auth guards, rate limiting) — broadening it further is not yet scheduled; treat "zero automated tests" as resolved for the auth-critical path specifically, not the whole app. **Update (2026-08-03 audit):** coverage has broadened substantially since this was written — 24 test files / 234 tests as of Part 16, including portfolio metrics, sheet sync, LP auth, document-access control, and DD-flow regression suites — but it is still narrow relative to the whole app (mostly `src/lib/**` pure-logic and route-handler unit tests, no component/e2e tests), so the underlying caveat still holds even though the specific numbers above are stale. **Update (2026-08-04 audit):** further grown to 25 test files / 273 tests; the underlying "not whole-app" caveat still holds.
>
> **Part 4 follow-up batch (WS6–WS8) shipped 2026-07-03.** Plans in Part 4 of `docs/IMPLEMENTATION_PLAN.md`; shipped feature detail folded into "Existing Features" above.
> - **WS6** — homepage nav collapsed to a single "Log in" link; "Investor access" moved to the footer
> - **WS7** — admin "Add Provider" flow, and real provider vetting restored (founder submissions stay Pending until an admin acts; endorsements are pure testimonials)
> - **WS8** — `/admin/updates` cross-portfolio feed of published updates (search, company filter, sort)

### P2 — Leverage (next quarter)

> **P2 batch shipped 2026-07-06.** WS9 (Fork Configuration) → WS10 (metric alerts) → WS11 (Scheduled Publishing) → WS12 (Bulk LP Link), planned in Part 5 of [`docs/IMPLEMENTATION_PLAN.md`](./docs/IMPLEMENTATION_PLAN.md), all shipped and folded into "Existing Features" above. **Mobile-Width Layout Hardening (WS14–WS15) shipped 2026-07-07**, planned in Part 6, also folded into "Existing Features" (Design & Branding) above. Comment Threading was deferred out of the batch (see its row below).

| Feature | Description | Benefits |
|---------|-------------|----------|
| **Comment Threading** | Threaded replies, @mentions, resolution status. (Basic comment notifications already shipped.) **Deferred from the Jul 2026 P2 batch** — it's the tier's only redesign of an existing shared surface (highest UX-regression risk), its notification half depends on the currently-broken Resend key, and @mentions need cross-company permission design first. Future sketch in Part 5 / WS13 of `docs/IMPLEMENTATION_PLAN.md`. | Turns one-way updates into a coaching feedback loop. Demoted from top priority: templates improve update quality more per unit of effort. |

### P3 — Later / Opportunistic

| Feature | Description | Benefits |
|---------|-------------|----------|
| **AI re-introduction** | Bring back portfolio-wide AI chat and add AI-assisted insights/alerts, with a cleaner implementation than the removed OpenAI RAG version. Digest drafting (Claude) already ships and can anchor the pattern. | Deliberately paused, not abandoned. Waits on a clean architecture decision. |
| **Metrics Benchmarking** | Anonymized peer comparison (e.g., "Your MRR growth is above portfolio median"). | Motivates founders; needs more portfolio data density to be meaningful. |
| **Portfolio Export / LP Report PDF** | Multi-company polished PDF for LP reporting. Goes beyond the single-update PDF today. **Resolved 2026-07-09 (F18):** the LP Fund-Report Portal (Part 7 of `docs/IMPLEMENTATION_PLAN.md`) shipped as the web-native successor to the PDF workflow — do not build a parallel PDF pipeline. Its print stylesheet (WS19, `/lp` report pages) is the shipped paper escape hatch: LPs use the browser's own Print-to-PDF. Revisit only if LPs demand a purpose-generated PDF beyond what browser printing gives them. | Streamlines DFS Lab's investor reporting workflow. |
| **Custom Company Tags** | Admin-created tags (e.g., B2B SaaS, female-led, export-ready) for portfolio segmentation. | Enables thematic analysis without building new dashboards. |
| **Update Versioning / Audit Trail** | Track edits to published updates. Who published/edited and when. | Compliance and data integrity as platform matures. Pairs with the admin audit log (shipped 2026-07-03) — same `AuditLog` model, add `UPDATE_EDITED` entries with a diff in `metadata`. |
| **Investor Accounts (Optional)** | Optional upgrade from token-only to persistent investor accounts. | Enables re-access, saved preferences, and engagement analytics. The `/investors` explainer page (shipped 2026-07-03) is the interim fix; this would replace it with real accounts. |
| **Slack / Webhook Integrations** | Slack notifications for overdue updates, new approvals, published updates. | Connects Molly into existing DFS Lab team workflows. |
| **Mobile-Optimized Update Flow** | Simplified metric entry and editor optimized for small screens — an *interaction redesign* of the founder update flow (composer, metric entry), not just layout. **Mobile-Width Layout Hardening shipped 2026-07-07** (see Design & Branding above) and already makes the existing update form and every other view render correctly at phone widths. **Annotated 2026-07-08:** the Medium-Style Draft Composer (see Founder Features above, shipped 2026-07-08, WS20) delivered the visual/layout half of a composer rethink; this item's remaining scope narrows to mobile-specific *interaction* work (chiefly simplified metric entry). | Many founders are mobile-first. Improves update frequency. |
| **Cmd+K command palette** | Keyboard-driven quick nav/search across the app, raised as a stretch idea during the Part 12 sidebar design review (Q36-A) but not a fix for any stated complaint. Someday note only — no plan, no decisions made. | Faster navigation for power users once the nav/portfolio surface is large enough to warrant it. |

---

## Notes

- Shipped features should be moved from the roadmap into the "Existing Features" section above.
- Priority tiers should be revisited quarterly or after each major release.
